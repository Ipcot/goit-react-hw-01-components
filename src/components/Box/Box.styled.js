import styled from 'styled-components';
import {
  typography,
  color,
  space,
  layout,
  flexbox,
  border,
  shadow,
} from 'styled-system';

export const Box = styled('div')(
  typography,
  color,
  space,
  layout,
  flexbox,
  border,
  shadow
);
